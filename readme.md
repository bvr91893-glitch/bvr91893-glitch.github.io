# bvr91893-glitch.github.io

This repository contains Bryan Roache's GitHub Pages site and the `cern-site/` project it links to.

## Open In GitHub Desktop

1. In GitHub Desktop, choose `File` > `Add Local Repository`.
2. Select `/Users/bryanroache/Desktop/ite170/bvr91893-glitch.github.io`.
3. Use the History and Changes tabs to review edits, commit, and publish them.

## Project Structure

- `index.html` is the main landing page.
- `cern-site/` contains the recreated CERN site pages.
- `assets/` stores shared images such as the site icon.
- `b_roache_cern_refactor-filetree.txt` contains the generated file tree snapshot for submission.

## Accessibility

The landing page uses semantic landmarks and a skip link to improve screen reader and keyboard navigation.
